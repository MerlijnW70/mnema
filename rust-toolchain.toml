# Pin the toolchain so a plain `cargo` (what internal-tool's gate invokes) resolves to a
# working compiler. mnema is edition 2024 — it needs Rust ≥ 1.85. 1.96 is the
# floor we build and gate against; the internal-tool mutation ratchet runs under it too.
[toolchain]
channel = "1.96"
