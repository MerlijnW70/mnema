# Pin the toolchain so a plain `cargo` (what the quality gate invokes) resolves to a
# working compiler. mnema is edition 2024 — it needs Rust ≥ 1.85. 1.96 is the
# floor we build and gate against; the mutation gate runs under it too.
[toolchain]
channel = "1.96"
