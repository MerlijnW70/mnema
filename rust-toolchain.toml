# Pin the toolchain so a plain `cargo` (what noha's gate invokes) resolves to a
# working compiler. engram is edition 2024 — it needs Rust ≥ 1.85. 1.96 is the
# floor we build and gate against; the noha mutation ratchet runs under it too.
[toolchain]
channel = "1.96"
